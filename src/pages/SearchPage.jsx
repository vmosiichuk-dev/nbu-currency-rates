import { RateListControls } from '@components/RateList/RateListControls';
import { RatesWidget } from '@components/RatesWidget/RatesWidget';

export const SearchPage = () => {
    return <RatesWidget RateListControls={RateListControls} />;
};
